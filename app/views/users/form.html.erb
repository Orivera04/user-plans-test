<% content_for :title do %>
  Usuarios
<% end %>

<%= render "shared/flash", flash: flash %>
<%= render partial: "users/user_form", layout: "layouts/form",
    locals: {
      record: @record,
      url: @url
    }
%>